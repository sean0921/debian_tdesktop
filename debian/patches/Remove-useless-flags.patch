Description: Remove unwanted compiler flags
 * -Werror flag is removed because of fatal warnings in Qt
 * Debian packages are compiled with second optimization level
 * Disable mandatory LTO
   - This is temporary workaround to avoid FTBFS on mips64el. For other
     architectures LTO is enabled by the d/rules file.
Author: Nicholas Guriev <guriev-ns@ya.ru>
Last-Update: 2017-09-14

--- a/Telegram/gyp/settings_linux.gypi
+++ b/Telegram/gyp/settings_linux.gypi
@@ -25,7 +25,6 @@
           '-pipe',
           '-g',
           '-Wall',
-          '-Werror',
           '-W',
           '-fPIC',
           '-Wno-unused-variable',
--- a/Telegram/gyp/telegram_linux.gypi
+++ b/Telegram/gyp/telegram_linux.gypi
@@ -72,18 +72,12 @@
     'configurations': {
       'Release': {
         'cflags': [
-          '-Ofast',
-          '-flto',
           '-fno-strict-aliasing',
         ],
         'cflags_cc': [
-          '-Ofast',
-          '-flto',
           '-fno-strict-aliasing',
         ],
         'ldflags': [
-          '-Ofast',
-          '-flto',
         ],
       },
     },
